{
  "name": "@a5af/agentmux",
  "version": "2.0.4",
  "description": "HTTP-based inter-agent messaging with MCP support",
  "private": true,
  "workspaces": [
    "server",
    "packages/agentmux-client"
  ],
  "scripts": {
    "build": "npm run build -w server && npm run build -w @a5af/agentmux-client",
    "build:server": "npm run build -w server",
    "build:client": "npm run build -w @a5af/agentmux-client",
    "start": "npm run start -w server",
    "dev": "npm run dev -w server"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/a5af/agentmux.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  }
}
